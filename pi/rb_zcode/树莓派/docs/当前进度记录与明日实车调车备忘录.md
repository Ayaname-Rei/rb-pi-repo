# 当前工作进度记录与调车备忘录 (2026-09-24)

## 一、今日已完成的核心工作与系统状态

### 1. 内存优化与系统瘦身（已生效）
- **永久关闭图形桌面**：执行了 `systemctl set-default multi-user.target`，并即时停止了 `lightdm`、`wayvnc`、`wayvnc-control`；
- **页面缓存瞬时回收**：执行了 `sync && echo 3 > /proc/sys/vm/drop_caches`；
- **当前实测系统内存状况**：
  - 总物理可用（用户空间）：**3.7 GiB**；
  - 已使用（Used）：已从 2.0 GiB 下降至 **1.0 GiB**；
  - 空闲物理内存（Free）：已从 408 MiB 飙升至 **2.4 GiB**；
  - 实际可用容量（Available）：达到 **2.7 GiB**；
  - 交换分区（Swap）：已用 0 MB。
- **软件保留说明**：
  - **VS Code Server**：目前保留以支持开发与调试，占 ~880MB；比赛前只需执行 `pkill -f vscode-server && rm -rf ~/.vscode-server` 即可彻底释放；
  - **Docker**：已完全保留，未做任何改动。

### 2. 全景问题代码修复与树莓派部署（全部通过测试）
- **发车与无头运行**：已完成 `start_trigger.py` 与 `run_autostart.py`，支持 50Hz 纯无头事件循环驱动；
- **视觉驱动升级**：重构 `vision/camera.py`（1280x720 强制锁定、MJPG 硬件压缩、丢弃 3 帧历史旧残影、NumPy 纯内存传输，彻底废弃 SD 卡临时图读写）；
- **底盘严格锁死**：在 `behavior_tree/vision_actions.py` 与 `behavior_tree/custom_actions.py` 中引入抓取/搭建前的底盘绝对停稳检测（`format_stop_cmd()` + `motion_state != 1`）；
- **战术大闭环**：在 `missions/main_mission.py` 段 18 后挂接了机械臂搭建动作；在 `config/mission_config.py` 中预置了 6 段返程轨迹；
- **实机通过验证**：`_sim_test_headless.py`（2 项通过 0 失败）、`_test_vision.py`（全流程通过）、`_test_vision_stub.py`（5 大用例全通过）、`run_autostart.py --sim`（54.7s 成功走完）。

---

## 二、明日工作规划与待执行项

1. **发车模块改动落地**：
   - 彻底移除 `start_trigger.py` 与 `run_autostart.py` 中对物理 GPIO 按键的依赖；
   - 适配为“SSH 终端回车触发 + 启动后一键拉入 `tmux` 会话托管”的发车流；
2. **USB 摄像头实机端口核对与标定**：
   - 将 USB 广角摄像头插上树莓派 USB 3.0/2.0 口；
   - 执行 `ls -la /dev/v4l/by-id/` 和 `v4l2-ctl --list-devices` 确认真实设备节点与 `Camera(index=0)` 对应关系；
   - 拍摄一张物料台实景照片，运行 `python3 -m vision.detect_image` 标定最新的 `(target_u, target_v)`；
3. **整车实物联调（按五阶段流程）**：
   - 阶段一：纯底盘实车空载 19 段跑位（接 `/dev/ttyUSB0`）；
   - 阶段二：接上 USB 相机与机械臂 `/dev/ttyUSB1`，测试工位视觉对准抓取；
   - 阶段三：全场 6 分钟大闭环跑表测试。
