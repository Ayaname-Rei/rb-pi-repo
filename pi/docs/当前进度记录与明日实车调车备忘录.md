# 当前工作进度记录与调车备忘录 (2026-09-24 晚更新)

## 一、已完成的核心工作与系统状态

### 1. 内存优化与系统瘦身（已生效并稳定运行）
- **永久关闭图形桌面**：执行了 `systemctl set-default multi-user.target`，并即时停止了 `lightdm`、`wayvnc`、`wayvnc-control`；
- **页面缓存瞬时回收**：执行了 `sync && echo 3 > /proc/sys/vm/drop_caches`；
- **树莓派当前实测状态**：
  - 总物理可用（用户空间）：**3.7 GiB**；
  - 已使用（Used）：已降至 **264 MiB**；
  - 空闲物理内存（Free）：高达 **3.0 GiB**；
  - 实际可用容量（Available）：高达 **3.4 GiB (占 92%)**；
  - CPU 核心温度：**29.7°C**，频控降频标记 `throttled=0x0`（完全无过热/降频风险）；
  - 交换分区（Swap）：已用 0 MB。
- **软件保留说明**：
  - **VS Code Server**：目前保留以支持开发与调试；比赛前只需执行 `pkill -9 -f vscode-server && rm -rf ~/.vscode-server` 即可彻底释放；
  - **Docker**：已完全保留，未做任何改动。

### 2. 比赛发车模块升级（按用户要求完成纯 SSH + tmux 重构）
- **移除物理按键**：彻底剥离了 `start_trigger.py` 中对物理 GPIO 按键及 `gpiozero` 的依赖；
- **纯 SSH 回车发车**：改为终端友好型交互，就位后在当前终端敲击【回车键 (Enter)】即可发车；
- **安装并配置 tmux**：在树莓派上安装了 `tmux 3.3a`，并编写了专属发车启动器 `launch_car.sh`：
  - 执行 `bash launch_car.sh` 会自动拉起名为 `car` 的 tmux 守护会话；
  - **网络免疫机制**：发车后即使拔掉网线、WiFi 丢包断连或关闭终端，小车后台进程绝不熄火，确保赛场安全稳定跑完全程；
  - **常用命令**：
    - 脱离会话保持后台运行：按 `Ctrl+B` 然后按 `D`；
    - 重新接入查看实时状态：`tmux attach -t car`；
    - 紧急刹停退出：在终端按 `Ctrl+C`。

### 3. 全景问题代码修复与树莓派部署（全部通过测试）
- **视觉驱动升级**：重构 `vision/camera.py`（1280x720 强制锁定、MJPG 硬件压缩、丢弃 3 帧历史旧残影、NumPy 纯内存传输，彻底废弃 SD 卡临时图读写）；
- **底盘严格锁死**：在 `behavior_tree/vision_actions.py` 与 `behavior_tree/custom_actions.py` 中引入抓取/搭建前的底盘绝对停稳检测（`format_stop_cmd()` + `motion_state != 1`）；
- **战术大闭环**：在 `missions/main_mission.py` 段 18 后挂接了机械臂搭建动作；在 `config/mission_config.py` 中预置了 6 段返程轨迹；
- **实机通过验证**：`_sim_test_headless.py`（全模块通过，2 项仿真测试 0 失败）、`_test_vision.py`（全流程通过）、`_test_vision_stub.py`（5 大用例全通过）、`run_autostart.py --sim`（成功走完）。

---

## 二、后续实车调车步骤（待硬件连线时执行）

1. **USB 摄像头实机端口核对与标定**：
   - 将 USB 广角摄像头插上树莓派 USB 口；
   - 执行 `v4l2-ctl --list-devices` 和 `ls -la /dev/v4l/by-id/` 确认真实设备节点与 `Camera(index=0)` 对应关系；
   - 拍摄一张物料台实景照片，运行 `python3 -m vision.detect_image <照片路径>` 标定最新的 `(target_u, target_v)`；
2. **底盘与机械臂串口接入测试**：
   - 底盘 A 板接 USB 口（识别为 `/dev/ttyUSB0`）；
   - 机械臂总线舵机板接 USB 口（识别为 `/dev/ttyUSB1`）；
   - 运行 `python3 test_arm_group.py --port /dev/ttyUSB1 --group 1` 验证舵机抓取动作组；
3. **整车实物联调（按五阶段流程）**：
   - 阶段一：纯底盘实车空载 19 段跑位（`bash launch_car.sh`）；
   - 阶段二：接上 USB 相机与机械臂，测试工位视觉对准抓取；
   - 阶段三：全场 6 分钟大闭环跑表实测。
